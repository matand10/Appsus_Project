
export function UnReadCount({ emails }) {
console.log(emails)
    // let counter
    // emails.find(email => {
    //     email.isRead = 'false'
    //     if (email.isRead = 'false') counter++
    // })
    // console.log(counter)
    return <section className="count-unread">

    </section>
}